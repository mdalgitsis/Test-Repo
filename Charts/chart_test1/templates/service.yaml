apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-test2.fullname" . }}
  labels:
    {{- include "chart-test2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-test2.selectorLabels" . | nindent 4 }}
